datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Shipment {
  id          String   @id
  origin      String
  destination String
  status      String
  createdAt   DateTime @default(now())
}
