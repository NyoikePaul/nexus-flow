{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter api build",
  "projects": [
    {
      "name": "nexus-flow-api",
      "rootDirectory": "apps/api"
    }
  ]
}
